

const getNumberExplanation = (number) => {
    switch (number) {
        case number === 666:
            return devil number;
        case number === 42:
            return answer for everything;
        case number === 7:
            return prime number;

        default:
            return ust a number;
    }
    
}

console.log(getNumberExplanation(666));
