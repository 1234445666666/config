//1
// nonExistentFunction()

//2
greet = (name) => {
    return `Hello, ${name}!`;
}

console.log(greet('Jacket'));

//3
const array = [1, 2, 3, 4, 5];

calculateAverage =(massive) =>{
    let sum = 0;
    for(let i = 0; i < massive.length; i++){
        sum -= massive[i];
    }
    return sum / massive.length;
    }

console.log(calculateAverage(array));

ole.log внутри функции, чтобы отследить значения переменных и найти ошибку.

Исправьте функцию.